{
  "name": "timet",
  "version": "0.3.0",
  "scripts": {
    "lint": "standard --fix",
    "test": "npm run lint && NODE_ENV=test jest --coverage",
    "test-windows": "npm run lint && set NODE_ENV=test && jest --coverage"
  },
  "standard": {
    "globals": [
      "beforeAll",
      "afterAll",
      "beforeEach",
      "afterEach",
      "describe",
      "it",
      "test",
      "expect"
    ]
  },
  "license": "MIT",
  "dependencies": {
    "lowdb": "^0.16.2",
    "minimist": "^1.2.0",
    "readline": "^1.3.0"
  },
  "devDependencies": {
    "coveralls": "^2.13.1",
    "jest": "^20.0.4",
    "standard": "^10.0.2"
  },
  "bin": "./bin/timet"
}
